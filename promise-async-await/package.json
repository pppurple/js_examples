{
  "name": "promise-async-await",
  "version": "1.0.0",
  "description": "promise,async-await example",
  "main": "asyncLoop.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-es2017": "^6.24.1"
  },
  "scripts": {
    "watch": "babel asyncLoopWithAsyncAwait.js --watch --out-dir dist",
    "build": "babel asyncLoopWithAsyncAwait.js --out-dir dist"
  }
}
